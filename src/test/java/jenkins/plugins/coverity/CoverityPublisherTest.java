package jenkins.plugins.coverity;

import hudson.model.FreeStyleProject;
import junit.framework.TestCase;
import org.jvnet.hudson.test.HudsonTestCase;

/**
 * @author Kohsuke Kawaguchi
 */
public class CoverityPublisherTest extends HudsonTestCase {
    /**
     * Work in progress configuration roundtrip testing.
     */
    public void testFoo() throws Exception {
//        FreeStyleProject p = createFreeStyleProject();
//        CoverityPublisher before = new CoverityPublisher("foo", null, "bar", "zot", true, null, null);
//        p.getPublishersList().add(before);
//
//        configRoundtrip(p);
//        CoverityPublisher after = p.getPublishersList().get(CoverityPublisher.class);
//
//        assertEqualDataBoundBeans(before,after);
    }
}
